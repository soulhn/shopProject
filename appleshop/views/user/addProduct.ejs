<!-- Product section-->
<section>
  <div class="container">
    <h1 class="display-5 fw-bolder text-center">상품등록</h1>
  </div>
</section>
<!-- Related items section-->
<section class="py-5 bg-light">
  <div class="container px-4 px-lg-5 mt-5">
    <form>
      <h2>상품정보</h2>
      <div class="mb-3">
        <label for="product" class="form-label">상품수량</label>
        <input type="number" class="form-control" id="exampleInputEmail1" aria-describedby="emailHelp" />
        <!-- <div id="ReqHelp" class="form-text">요청사항을 입력해주세요</div> -->
      </div>
      <div class="mb-3">
        <label for="주소" class="form-label">상품가격</label>
        <input type="number" class="form-control" />
        <div id="ReqHelp" class="form-text">10000원일 경우 10000으로 입력해주세요</div>
      </div>
      <div class="mb-3">
        <label for="주소" class="form-label">상품상세설명</label>
        <input type="text" class="form-control" />
      </div>

      <div class="mb-3"><img id="preview" /></div>
      <div class="mb-3">
        <label for="주소" class="form-label">상품이미지</label>
        <input type="file" class="form-control" onchange="readURL(this);" />
      </div>

      <button type="submit" class="btn btn-primary">상품등록하기</button>
    </form>
  </div>
  <script>
    function readURL(input) {
      if (input.files && input.files[0]) {
        var reader = new FileReader();
        reader.onload = function (e) {
          document.getElementById("preview").src = e.target.result;
        };
        reader.readAsDataURL(input.files[0]);
      } else {
        document.getElementById("preview").src = "";
      }
    }
  </script>
</section>
